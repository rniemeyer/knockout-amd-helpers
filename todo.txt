using require config
additional flexibility
  teardown function
header, etc.
Nuget
work on a decent example (doesn't need to show every option)
work on blog post
test baseDir in modules
foreach possible if module returns array (is this necessary)
tests for observable module, module name, module template
maybe do better test for inline template, check all childnodes for nodeType